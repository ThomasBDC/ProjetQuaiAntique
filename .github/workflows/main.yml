- name: FTP Deployment
  # You may pin to the exact commit or the version.
  # uses: airvzxf/ftp-deployment-action@9efe025c218094bd451a0cb684d7cb471f4d9d4f
  uses: airvzxf/ftp-deployment-action@latest
  with:
    # FTP Server.
    server: quaiantiquerestaurant.alwaysdata.net
    # FTP User.
    user: quaiantiquerestaurant
    # FTP Password.
    password: K@n@be@ch1997
    # Remote directory.
    remote_dir: /www/
    # Delete all the files inside of the remote directory.
    delete: true
